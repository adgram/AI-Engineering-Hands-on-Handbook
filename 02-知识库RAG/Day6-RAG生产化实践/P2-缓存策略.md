# P2：缓存策略 — LLM 缓存 + Embedding 缓存

## 目标
通过缓存降低 API 调用成本，提升响应速度

> **三类缓存说明**：
> - 本文聚焦 RAG 应用层缓存（重复查询/嵌入复用）
> - KV Cache（推理加速）见 Prompt D2-P1（本质不同：后者是 Transformer 推理优化，无需额外代码实现）
> - Agent 状态持久化（SQLite/Redis/断点恢复）见 Agent D10-P1

|          | 学习路径                                                     |
| -------- | ------------------------------------------------------------ |
| 已有基础 | Day1-P1（Token 成本概念）+ Day4-P1（NaiveRAG 每次都调用 API） |
| 本章内容 | 落地三级缓存（Embedding 缓存/LLM 精确响应缓存/语义缓存），降低重复查询的 API 成本和响应延迟。 |

## 为什么需要缓存？

缓存的思路很直观：对于相同的查询，直接返回之前的结果，避免重复调用 API。下面用一张图对比有/无缓存的流程差异。

```
无缓存：每次相同问题都调用 API → 重复花钱、慢
有缓存：相同问题直接返回 → 免费、快
```

## 策略 1：Embedding 缓存

Embedding 缓存将文本的向量化结果存储到磁盘，相同的文本再次请求时直接读取缓存，避免重复调用 Embedding API。

```python
import hashlib
import json
import diskcache

class EmbeddingCache:
    def __init__(self, cache_dir="./cache/embeddings"):
        self.cache = diskcache.Cache(cache_dir)
    
    def _key(self, text: str, model: str) -> str:
        return hashlib.md5(f"{model}:{text}".encode()).hexdigest()
    
    def get(self, text: str, model: str = "BAAI/bge-m3") -> list:
        key = self._key(text, model)
        return self.cache.get(key)
    
    def set(self, text: str, embedding: list, model: str = "BAAI/bge-m3"):
        key = self._key(text, model)
        self.cache.set(key, embedding)
    
    def get_or_compute(self, text: str, compute_fn, model: str = "BAAI/bge-m3") -> list:
        cached = self.get(text, model)
        if cached is not None:
            return cached
        embedding = compute_fn(text)
        self.set(text, embedding, model)
        return embedding

# 使用
embed_cache = EmbeddingCache()

def get_embedding(text):
    response = client.embeddings.create(model="BAAI/bge-m3", input=text)
    return response.data[0].embedding

# 第一次：调用 API
emb1 = embed_cache.get_or_compute("RAG 是什么？", get_embedding)
# 第二次：直接返回缓存
emb2 = embed_cache.get_or_compute("RAG 是什么？", get_embedding)
print(f"相同结果: {emb1 == emb2}")
```

## 策略 2：LLM 响应缓存（完全一致）

LLM 响应缓存以输入消息、模型和温度参数为 key，对完全相同的请求直接返回历史响应结果，适用于 temperature=0 的确定性场景。

```python
class LLMResponseCache:
    def __init__(self, cache_dir="./cache/llm"):
        self.cache = diskcache.Cache(cache_dir)
    
    def _key(self, messages: list, model: str, temperature: float) -> str:
        data = json.dumps({"messages": messages, "model": model, "temperature": temperature}, ensure_ascii=False, sort_keys=True)
        return hashlib.sha256(data.encode()).hexdigest()
    
    def get_or_compute(self, messages: list, compute_fn, model: str = "deepseek-v4-flash", temperature: float = 0) -> str:
        key = self._key(messages, model, temperature)
        cached = self.cache.get(key)
        if cached is not None:
            return cached
        result = compute_fn(messages)
        self.cache.set(key, result)
        return result

# 使用
llm_cache = LLMResponseCache()

def call_llm(messages):
    response = client.chat.completions.create(model="deepseek-v4-flash", messages=messages, temperature=0)
    return response.choices[0].message.content

messages = [{"role": "user", "content": "RAG 是什么？"}]

# 第一次：调用 API
ans1 = llm_cache.get_or_compute(messages, call_llm)
# 第二次：直接返回缓存
ans2 = llm_cache.get_or_compute(messages, call_llm)
print(f"缓存命中: {ans1 == ans2}")
```

## 策略 3：语义缓存（Semantic Cache）

相同问题但表述不同时也命中缓存：

```python
class SemanticCache:
    def __init__(self, collection, threshold: float = 0.3):
        self.collection = collection
        self.threshold = threshold
    
    def lookup(self, query: str) -> str:
        results = self.collection.query(query_texts=[query], n_results=1)
        if results['documents'][0] and results['distances'][0][0] < self.threshold:
            return results['metadatas'][0][0].get('answer')
        return None
    
    def store(self, query: str, answer: str):
        self.collection.add(
            documents=[query],
            metadatas=[{"answer": answer}],
            ids=[hashlib.md5(query.encode()).hexdigest()]
        )

# 测试
sem_cache = SemanticCache(
    chromadb.PersistentClient(path="./cache/semantic").get_or_create_collection("qa_cache")
)

q = "RAG 的优点是什么？"
answer = sem_cache.lookup(q)
if answer:
    print(f"语义缓存命中: {answer}")
else:
    answer = "减少幻觉、支持最新知识、可追溯"
    sem_cache.store(q, answer)
    print(f"已缓存")

# "RAG 有哪些好处？" → 应该命中缓存
similar = "RAG 有哪些好处？"
answer = sem_cache.lookup(similar)
print(f"相似查询{'命中' if answer else '未命中'}: {answer}")
```

## 缓存分级策略

将缓存分为 L1 精确缓存和 L2 语义缓存两级，逐级回退，在命中率和计算开销之间取得平衡。

```
请求到达 → L1: 精确缓存（完全相同查询）
          → L2: 语义缓存（相似查询）
          → L3: 调用 API
          → 结果存入 L1 + L2
```

> Token 管理和成本优化的完整方案详见 P3 | Token 基础概念见 Prompt D1-P1

## 缓存失效策略

缓存数据需要合理的失效策略来保证数据新鲜度，下表对比了四种常见的失效策略及其适用场景。

| 策略 | 说明 | 适用 |
|------|------|------|
| TTL（定时过期） | 设置过期时间 | 数据频繁更新 |
| LRU（最近最少使用） | 淘汰最久未用的 | 缓存空间有限 |
| 主动失效 | 数据更新时主动删除 | 数据可追踪 |
| 版本控制 | 缓存 key 带版本号 | 系统升级 |

## 动手实验

1. 实现精确缓存和语义缓存
2. 对比有/无缓存的响应时间和成本
3. 设置合理的 TTL（根据你的数据更新频率）
4. 统计缓存命中率

## 完成标准
- [ ] 理解缓存的必要性
- [ ] 实现至少两种缓存策略
- [ ] 量化了缓存的收益
- [ ] 制定了缓存失效策略

## 下一步 → P3-成本优化.md
