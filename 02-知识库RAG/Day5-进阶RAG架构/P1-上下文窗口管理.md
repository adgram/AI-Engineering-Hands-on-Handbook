# P1：上下文窗口管理 — 动态 Top-K 与 Score 阈值

## 目标
掌握检索结果的动态过滤策略，避免"噪声淹没信号"

> **前置知识**：GSSC（Gather-Select-Structure-Compress）框架见 Prompt D2-P1。本篇是 GSSC 中 Select 步骤在 RAG 场景的延伸——对检索结果做动态过滤和数量选择。

|          | 学习路径                                                     |
| -------- | ------------------------------------------------------------ |
| 已有基础 | Day2-P1（GSSC 框架 Select 步骤）+ Day4-P2（上下文注入策略） |
| 本章内容 | 落地三种动态过滤策略（绝对阈值/动态 Top-K/相对距离比），让 Top-K 从固定值变成根据查询自适应的动态值。 |

## 问题

固定 Top-K 无法适应不同查询的质量差异：当检索质量高时多余的 Top-K 结果会引入噪声，质量低时又可能遗漏相关信息。

```
检索结果：距离越近越相关
但：有时候 Top-5 都相关，有时候 Top-1 之外都是噪声
固定 Top-K 不够灵活
```

## 策略 1：Score 阈值过滤

为检索结果的分数或距离设置固定阈值，低于（或高于）该阈值的结果被丢弃，确保只有高质量片段进入上下文。

```python
def threshold_filter(results: dict, min_score: float = None, max_distance: float = None) -> dict:
    """根据分数或距离过滤结果"""
    if not results['documents'][0]:
        return results
    
    filtered = {"documents": [[]], "metadatas": [[]], "distances": [[]], "ids": [[]]}
    
    for i, (doc, meta, dist, doc_id) in enumerate(zip(
        results['documents'][0],
        results['metadatas'][0],
        results['distances'][0],
        results['ids'][0]
    )):
        keep = True
        if max_distance is not None and dist > max_distance:
            keep = False
        if keep:
            filtered['documents'][0].append(doc)
            filtered['metadatas'][0].append(meta)
            filtered['distances'][0].append(dist)
            filtered['ids'][0].append(doc_id)
    
    return filtered

# 使用：只保留距离 < 0.5 的结果
raw_results = collection.query(query_texts=["RAG 是什么？"], n_results=10)
filtered = threshold_filter(raw_results, max_distance=0.5)
print(f"过滤前: {len(raw_results['documents'][0])} 条")
print(f"过滤后: {len(filtered['documents'][0])} 条")
```

## 策略 2：动态 Top-K

不固定返回数量，而是根据检索分数的突变点自动截断——当连续结果的距离差超过阈值时，认为后续结果已不相关。

```python
def dynamic_top_k(results: dict, min_docs: int = 1, max_docs: int = 10, distance_gap: float = 0.1) -> dict:
    """
    动态选择结果数量：
    - 最少返回 min_docs 条
    - 最多返回 max_docs 条
    - 如果下一个结果和上一个的距离差大于 gap，则截断
    """
    if not results['documents'][0]:
        return results
    
    docs = results['documents'][0]
    distances = results['distances'][0]
    
    # 始终保留至少 min_docs 条
    keep_count = min_docs
    
    # 检查距离突变
    for i in range(min_docs, min(len(distances), max_docs)):
        if i >= 1 and distances[i] - distances[i-1] > distance_gap:
            # 距离突变，说明后面的都不相关了
            break
        keep_count = i + 1
    
    return {
        "documents": [docs[:keep_count]],
        "metadatas": [results['metadatas'][0][:keep_count]],
        "distances": [distances[:keep_count]],
        "ids": [results['ids'][0][:keep_count]]
    }

# 测试
raw_results = collection.query(query_texts=["RAG 是什么？"], n_results=10)
dynamic = dynamic_top_k(raw_results, min_docs=2, max_docs=10, distance_gap=0.15)
print(f"原始 10 条距离: {[f'{d:.4f}' for d in raw_results['distances'][0]]}")
print(f"动态选择 {len(dynamic['documents'][0])} 条")
```

## 策略 3：相对距离比

以最佳匹配结果的距离为基准，按倍数设定动态截止线，自适应不同查询的分数分布。

```python
def relative_distance_filter(results: dict, ratio_threshold: float = 2.0) -> dict:
    """
    仅保留距离 <= 最佳距离 * ratio_threshold 的结果
    例如：最佳距离 0.2，ratio=2.0 → 保留距离 <= 0.4 的所有结果
    """
    if not results['documents'][0]:
        return results
    
    best_distance = results['distances'][0][0]
    cutoff = best_distance * ratio_threshold
    
    filtered = {"documents": [[]], "metadatas": [[]], "distances": [[]], "ids": [[]]}
    
    for doc, meta, dist, doc_id in zip(
        results['documents'][0], results['metadatas'][0],
        results['distances'][0], results['ids'][0]
    ):
        if dist <= cutoff:
            filtered['documents'][0].append(doc)
            filtered['metadatas'][0].append(meta)
            filtered['distances'][0].append(dist)
            filtered['ids'][0].append(doc_id)
    
    return filtered
```

## 三种策略对比

以下表格从优点、缺点和适用场景三个维度对比三种过滤策略，帮助根据实际需求选择。

| 策略 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 绝对阈值 | 简单直接 | 阈值难确定，不同查询差异大 | 数据分布稳定 |
| 动态 Top-K | 自动适应查询 | 依赖 gap 参数 | 通用 |
| 相对距离比 | 自适应不同查询 | 最佳结果可能不理想 | 长尾分布 |

> 延伸阅读：GSSC 框架的完整 Select 策略（相关性评分/预算控制）详见 Prompt D2-P1

## 动手实验

1. 找一个查询，对比三种策略的不同效果
2. 调整参数，观察过滤结果的变化
3. 在你的 RAG Pipeline 中加入动态 Top-K
4. 评估使用动态过滤后对最终回答质量的影响

## 完成标准
- [ ] 实现至少两种上下文窗口管理策略
- [ ] 理解固定 Top-K 的问题
- [ ] 优化了自己的 RAG Pipeline

## 下一步 → (P2-多轮对话中的RAG)[P2-多轮对话中的RAG.md]
